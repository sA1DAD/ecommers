document.addEventListener('DOMContentLoaded', () => {
  const container = document.getElementById('orders-container');
  if (container) container.innerHTML = '<div class="alert alert-info">Order history is not implemented yet.</div>';
});
