document.addEventListener('DOMContentLoaded', () => {
  const form = document.getElementById('checkoutForm');
  if (form) {
    form.innerHTML = '<div class="alert alert-warning">Checkout is still under development.</div>';
  }
});
