document.addEventListener('DOMContentLoaded', () => {
  const container = document.getElementById('salesContainer');
  if (container) container.innerHTML = '<div class="alert alert-warning">Seller sales are not implemented yet.</div>';
});
